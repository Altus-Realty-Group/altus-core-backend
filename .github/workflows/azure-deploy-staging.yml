name: RETIRED — Legacy Azure deployment disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked-retired-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy deployment authority
        shell: bash
        run: |
          echo "Deployment is disabled in Altus-Realty-Group/altus-core-backend."
          echo "Canonical backend and deployment governance moved to Altus-Realty-Group/altus-control-plane."
          echo "Retirement tracking: https://github.com/Altus-Realty-Group/altus-control-plane/issues/7"
          exit 1
